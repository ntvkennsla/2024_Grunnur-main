import 'dart:io';
void main(){
  /*
  int tax = 24.4;
  int salary = 0;
  print("What is my salary + tax!");
  print("what is you salary?");
  String? input = stdin.readLineSync(); //get keyboard string
  int numberInput = int.tryParse(input!) ?? 0; //move string to int to calc
  salary = numberInput - tax;
  print("my salary is:");
  print(salary);
  // næsta verkefni alveg eins bara nota double i stað int
  // og vera með 3.5 stettarfelag i stað tax
  // bonus verkefni - vera með bæði tax(skatman) og stettarfelag
  // reikna utborgun
*/
}