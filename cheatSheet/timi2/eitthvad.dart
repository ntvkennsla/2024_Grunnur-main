import 'dart:io';
void main(){
  // næsta verkefni alveg eins bara nota double i stað int
  // og vera með 3.5 stettarfelag i stað tax
  // bonus verkefni - vera með bæði tax(skatman) og stettarfelag
  // reikna utborgun
    double stetf = 3.5;
    double salary = 0.0;

    // Boolean is either true or false - always
    bool? trunadur = true;
    bool? falsari;
    print(trunadur);
    print(falsari);
}